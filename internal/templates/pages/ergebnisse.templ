package ui_pages

import "github.com/bata94/RegattaApi/internal/templates/components"

templ Ergebnisse() {
  @ui_components.LandingPageDivCol() {
    @ui_components.PageHeader(
      "Ergebnisse",
    )
    @ui_components.PageHeaderSubtext(
      `Hier wirst du zeitnah, nach dem Zieleinlauf die Ergebnisse der Rennen
      abrufen können. Aktuell ist aber noch etwas Zeit, komme also an den
      Regattatagen wieder :)`,
    )
  }
}
